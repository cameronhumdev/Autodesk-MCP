# OpenAI-compatible endpoint (Ollama default)
LLM_BASE_URL=http://127.0.0.1:11434/v1
LLM_MODEL=llama3.2
LLM_API_KEY=

# Use "demo" to run without a real LLM (mock replies + tools still work)
LLM_MODE=auto
# auto | live | demo

RAG_BACKEND=local
HOST=0.0.0.0
PORT=8080
